# TODO:
# Default profile photos: make them the same sizes as the pivots photos. Copy them into the public directory.
#
# Basically, emulate what happens when you run script/generate migration and resource.

require File.dirname(__FILE__) + '/plugin_installer'

PluginInstaller::Installer.new.run