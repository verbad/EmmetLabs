#admin
  %h1 Welcome back!
  %p
    You have registered with the email address
    %i= @token.email_address
    - if @token.tokenable.verified?
      %p
        Your address has already been verified! Congratulations.
        = link_to 'Click here', new_login_path
        to log in.
    - else
      %p
        To start using your address, just click the button below.
        - form_for :email_address_verification_token, :url => email_address_verification_request_path(@user, @email_address, @token), :html => {:method => :put} do |form|
          = submit_tag 'Complete the registration process', :id => 'complete_process_button'
